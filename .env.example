# BİGR Discovery environment variables

# Database (choose one):
DATABASE_URL=sqlite:///~/.bigr/bigr.db
# DATABASE_URL=postgresql://user:pass@ep-xxx.eu-central-1.aws.neon.tech/bigr_discovery?sslmode=require

# CORS allowed origins (comma-separated, required for separate frontend deploy)
# CORS_ORIGINS=https://bigr-discovery.onrender.com

# Debug mode (optional, also enables localhost:5173 CORS)
# DEBUG=true

# Agent registration secret (optional — if set, agents must provide it to register)
# AGENT_REGISTRATION_SECRET=your-secret-here
